@model  IList<BetterSite.Domain.M_Tags>
@{
    Layout = null;
    var sucai = Model.Where(m => m.TypeCode.ToLower() == "sucai");
    var gongju = Model.Where(m => m.TypeCode.ToLower() == "gongju");
    var haowan = Model.Where(m => m.TypeCode.ToLower() == "haowan");
    var chaxun = Model.Where(m => m.TypeCode.ToLower() == "chaxun");
    var tisheng = Model.Where(m => m.TypeCode.ToLower() == "tisheng");
    var zhuanlan = Model.Where(m => m.TypeCode.ToLower() == "zhuanlan");
}

<style>
        #menu span{
    font-size: 12px;
    /*font-weight: bolder;*/
    width:200px;
    display:block;
    border-bottom-width: 1px;
    border-bottom-style: solid;
    border-bottom-color: #ccc;
    }
    #menu li{
    list-style-image: none;
    list-style-type: none;
    margin:7px 7px 3px 7px;
    /*background-color: #63c3e8;
    border-right-width: 2px;
    border-right-style: solid;
    border-right-color: #fff;*/
    float: left;
    font-size:13px;
    }
            #menu ul {
            width:200px;
            }
    #menu li a{
    /*color: #555;
    text-decoration: none;*/
    margin: 0px;

    /*display: block;*/ /* 作为一个块 */
    /*padding-right: 5px;
    padding-left: 5px;
    padding-bottom: 5px;
    padding-top: 5px;*/

    }
     #menu li a:hover,
    #menu1 li.active a{
         color: #555;
         /*display: block;*/ /* 作为一个块 */

    /*background-color: #97d2fa;*/
    /*border-top-width: 1px;
    border-top-style: solid;
    border-top-color: #fff;*/
    }
            .navdiv {
                border: 1px dotted #97d2fa;/*solid 1px #97d2fa;*/
        display: inline-block;
        width: 216px;
        padding: 10px;
            }
    #menu li h3 {
    margin:0px;
    }
</style>
<ul id="menu">
    <li class="">
        <h3><span>免费素材</span></h3>
            <ul class="">
                @foreach (var item in sucai)
            {
                    <li><a href="/sucai/@item.TagName/1.html" title="@item.TagName">@item.TagName</a></li>
                }
            </ul>
</li>
    <li class="">
        <h3><span>在线工具</span></h3>
        <ul class="">
            @foreach (var item in gongju)
            {
                <li><a href="/gongju/@item.TagName/1.html" title="@item.TagName">@item.TagName</a></li>
            }
        </ul>
    </li>
    <li>
        <h3><span>发现好玩</span></h3>
        <ul class="">
            @foreach (var item in haowan)
            {
                <li><a href="/haowan/@item.TagName/1.html" title="@item.TagName">@item.TagName</a></li>
            }
        </ul>
    </li>
    <li>
        <h3> <span>便民查询</span></h3>
        <ul class="">
            @foreach (var item in chaxun)
            {
                <li><a href="/chaxun/@item.TagName/1.html" title="@item.TagName">@item.TagName</a></li>
            }
        </ul>
    </li>
    <li>
        <h3><span>个人提升</span></h3>
    <ul class="">
        @foreach (var item in tisheng)
            {
            <li><a href="/tisheng/@item.TagName/1.html" title="@item.TagName">@item.TagName</a></li>
        }
    </ul>
</li>
    <li>
        <h3><span>行业专栏</span></h3>
        <ul class="">
            @foreach (var item in zhuanlan)
            {
                <li><a href="/zhuanlan/@item.TagName/1.html" title="@item.TagName">@item.TagName</a></li>
            }
        </ul>
    </li>
</ul>
@*<ul id="menu" >
        <li  class=""><a id="tab1" href="#" >免费素材</a></li>
        <li  class="active"><a id="tab2"  href="#">在线工具</a></li>
        <li ><a  id="tab3" href="#">发现好玩</a></li>
        <li><a  id="tab4" href="#">便民查询</a></li>
    </ul>
    <div class="navdiv">
        <ul class="sb_menu tab1">
            <li> 免费素材</li>
            @foreach (var item in Model)
            {
                <li><a href="?Tag=@item.TagName" title="@item.TagName">@item.TagName</a></li>

            }
        </ul>
        <ul class="sb_menu tab2">
            <li>在线工具</li>
            @foreach (var item in Model)
            {
                <li><a href="?Tag=@item.TagName" title="@item.TagName">@item.TagName</a></li>

            }
        </ul>
        <ul class="sb_menu tab3">
            @foreach (var item in Model)
            {
                <li><a href="?Tag=@item.TagName" title="@item.TagName">@item.TagName</a></li>

            }
        </ul>
        <ul class="sb_menu tab4">
            @foreach (var item in Model)
            {
                <li><a href="?Tag=@item.TagName" title="@item.TagName">@item.TagName</a></li>

            }
        </ul>
    </div>*@

@section scripts {
    @*<script type="text/javascript">
                function areaover(n) {
                    debugger;
                    $('.sb_menu').css('display', 'none');
                    $('.sb_menu .' + n).css('display', 'block');
                }
        </script>*@

}
